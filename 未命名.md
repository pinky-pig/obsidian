```ts
withDefaults(
  defineProps<{
    // 是否显示关闭按钮
    isShowClose?: boolean
    // 对其方式
    align?: 'left' | 'right'
    // 倾斜方式
    rotateMode?: 'left' | 'right'
    // 距离
    alignOffset?: number
    // 距离顶部
    topOffset?: number
  }>(),
  {
    isShowClose: false,
    align: 'left',
    rotateMode: 'left',
    alignOffset: 40,
    topOffset: -180,
  },
)
```